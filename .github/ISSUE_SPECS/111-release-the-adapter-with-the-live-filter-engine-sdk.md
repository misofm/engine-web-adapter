# Release the adapter with the live-filter engine SDK

Astra XHIGH approved scope; user requests bounded Luna XHIGH implementation and independent Astra MEDIUM verification. Prerequisite: misofm/engine#809 verified immutable SDK release including #147/#804 capabilities. Starting main d2492ef898c9285864eebf6903503a5fb73069e3; refresh before implementation. Work in isolated /tmp/miso-adapter-804, preserve primary research work.

## Adapter child: adopt the exact released SDK

The adapter already exposes the same borrowed `session.engine`; no new cut wrapper, analysis delegate, decoder/backend/cache behavior or second engine is needed. After the accepted SDK registry identity exists, update exact dependency and unused adapter release version in `package.json`, `package-lock.json`, existing `.github/workflows/npm-publish.yml` guards, `src/provenance.ts`, `scripts/check-package.mjs`, `tests/foundation.test.ts`, `NOTICE` and `README.md` only where their current exact pins require it. Re-audit version availability at freeze; preserve 0.5.8 immutable bytes.

Run existing `npm run check`, `npm run publish:dry-run`, packed browser `node scripts/browser-packed.mjs --indexed-sparse` and required macOS Chromium/WebKit OPFS qualification. Add a bounded new-cut call to the existing public-console/type or packed scenario if the app browser gate does not already exercise that exact archive. Require an ordinary fresh tarball consumer installation with exactly one SDK resolution and public imports/types, followed by the same proof from the actual registry archive. Existing Worker/Worklet URLs and same-engine ownership must remain valid after packing. Do not rely on the packed harness's copied development node_modules as proof of registry dependency resolution.


## Bounded delivery

One dependency/provenance adoption tranche, then root checkpoint and focused source/package review. Root runs existing required CI and macOS OPFS qualification, publishes the exact accepted archive through existing OIDC workflow, verifies actual registry archive/provenance and a fresh one-SDK consumer. Do not republish following ambiguous publication: verify the original qualified archive. Do not replace immutable 0.5.8 or choose a new version until registry availability is checked at freeze. App adoption follows verified publication; no new adapter control facade is required.

## Evidence

Implementation, qualification and publication pending. Record accepted source SHA, SDK and adapter versions, archive hashes, registry provenance, fresh consumer result and workflow IDs before closure.

### Accepted dependency and release freeze

Root refreshed origin/main (unchanged d2492ef8) and audited all49 local numbered
specs against56 remote issues: none missing; #111 number/title/open state match.
Engine SDK **0.3.0** is published and verified from source
`51e03cfdde61802fc8456872a6637a265fae5979`. Required mainCI34880226665 and
immutable qualification34880929687 pass. Publication34881586727 was accepted by
npm but exceeded registry propagation time; verify-only34881963287 then passed
fresh public imports/CLI and cryptographic SLSA archive/source/workflow checks,
without republishing. Root independently downloaded the registry archive: SHA256
`0da5f34d6d5e021f543acf21053b6176931548f3bbc584301c42c2f278f5b804`, integrity
`sha512-56Xwu7K/k7gRSGGLpNz7Vo1WSwaaP2F2to6Ahb/iMZM21GTFsgk24ZXVTVpdVsG1Q8fme/VbEPNjtm50tpRFLQ==`.
Wasm remains7e925d939234b67d14be41a647b4cc6de23099501a763d27e8a56c77524999e7.

Root freezes unused adapter **0.5.9** after a fresh registry version check
(latest0.5.8). Luna XHIGH may implement the one exact-dependency/provenance
tranche now. Existing local Emscripten6.0.9 and pinned Rust/browser tools are
available. Keep runtime/API behavior unchanged; app#222 owns the live-cut browser
episode against the same SDK bytes. Fresh tarball consumer, existing checks and
packed indexed-sparse proof remain; root owns review, macOSCI and publication.
