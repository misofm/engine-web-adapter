# Publish accepted sparse adapter APIs against Engine SDK 0.2.3

Astra medium scoped this narrow release in SDK_ADOPTION_SCOPE.md; root rechecked the completed engine753 registry evidence and accepted adapter66. Luna xhigh implements; fresh Astra medium verifies one frozen candidate. Maximum two coherent implementation attempts; stop and rebrief on a second failure. This is package identity/provenance delivery, not a new audio or storage feature. Root owns exact-path checkpoints, prompt pushes, publication, GitHub synchronization and cleanup.

## Frozen prerequisites

Adapter66 implementation777d785dcfd48ec70584568517855151fcc516bc has fresh independent attempt3 PASS, full278-test and actual packed Chromium qualification. It merged through PR67 at0d7b61b7b523b07c15bfd0040386882bd931f65f and issue66 is closed; require its automatically triggered macOS OPFS run34642622947 to finish successfully before implementation begins. Accepted56/58/60/62/64 capabilities and the private decoder source seam are included. Indexed full-response acquisition remains separate.

Engine753 is complete and independently verified as public @misofm/engine@0.2.3, exact release source0d9af85d9cfeb8ccb2567aea26bdcf284a73fa0c. Registry URL https://registry.npmjs.org/@misofm/engine/-/engine-0.2.3.tgz; SHA2562fd0f6f6bc4c7e311fdd10610c6c26de810e564a89ad912fc1961bebd8d5c590; shasum3f039cc83e5eed16d02c32e5683cbf2d81c91943; integrity sha512-PkPN5YbEnTmwEfexRPPX5ijkd1/FOpTlvbar+d5SPJtCbHrnSrK7EEbRksavDbK8wTUU/VOhnucALSD+Nwqi3w==. Worklet SHA2560c633b091457c4ab9ec17f08327802f423874124182bc794a7988ed1c536fa73. Published once; unchanged registry/signature/SLSA assertions independently passed read-only recovery after candidly recorded automation propagation/dry-run failures. Evidence at /data/sparse-pcm-launch/tooling/engine-753/. Do not substitute the later documentation merge for the published source identity.

Root queried registry adapter0.3.6 at2026-09-11T20:07Z and obtained parsed E404. Recheck before publication; a transport/auth/ambiguous failure is never absence. Create an isolated worktree from freshly fetched clean main; preserve exact Effect4.0.0-rc.112 and all other dependency versions.

## Boundary A: adapter dependency and intermediate registry release

Smallest outcome: public @misofm/engine-web-adapter@0.3.6 imports successfully, depends exactly on registry @misofm/engine@0.2.3, carries truthful SDK provenance and exposes the already accepted sparse APIs while legacy native-range sessions still work. It does not claim full-GET sparse acquisition or end-to-end offline app support.

Expected edits only:

- package.json version0.3.6 and exact SDK dependency0.2.3; package-lock.json root/version/dependency/resolved integrity regenerated from npm without unrelated dependency upgrades.
- src/provenance.ts engine.package, archiveSha256 and commit from the VERIFIED753 registry artifact/release. Preserve historical safeBaselines and copiedSources, which document actual earlier adaptations rather than current dependency identity.
- scripts/check-package.mjs exact dependency assertion from0.2.2 to0.2.3, retaining ring/asset identity checks and package policy.
- README.md install examples, current SDK pin/archive/source provenance and concise release scope. Historical issue records are not search-and-replaced.
- .github/workflows/npm-publish.yml PACKAGE_VERSION/job display name0.3.6 and all SDK dependency guard/registry checks0.2.3.
- The new issue/evidence record. Any source/test correction requires a concrete failing existing consumer gate and an explicit smallest-path issue amendment. No speculative SDK compatibility shim, new API, DSP, graph, storage algorithm or runtime framework.

Retain existing publication mechanism and protections: workflow_dispatch on main, exact40-character expected_sha equal to dispatch/checkout, serialized noncancelling publication, pinned npm client, immutable version parsed-E404 refusal, OIDC-only/no token fallback, bounded registry propagation, fresh public consumer imports and `npm audit signatures --include-attestations`. Keep publish and verify modes. Do not replace this with a new qualification/promotion system or weaken guards to release from a feature ref. Reserve the brief current-main publication window; if main advances before dispatch, recheck the actual current source and qualify its release identity rather than pretending an old SHA is still dispatchable.

Gates on the frozen release candidate:

1. `npm ci --ignore-scripts`, then existing `npm run check`: formatting, typecheck/source policy, decoder provenance, full tests and package policy. Resolve exactly SDK0.2.3 and Effectrc112; inspect lock delta for unrelated upgrades. Keep package-owned ring/asset identity assertions.
2. Pack using existing tooling and run the existing fresh consumer/packed browser test. Demonstrate public root, /stems and /assets exports, accepted sparse parser roundtrip/session/pump behavior and legacy native FLAC cold/warm session behavior with actual packaged workers/worklet. A compact public-consumer headless empty-rack versus bypassed-compressor fixture must compare unshifted output from sample zero with the installed0.2.3 artifact; reuse753/app187's discriminator if available rather than creating another harness. Existing source acceptance is not enough when the dependency binary changes.
3. Fresh Astra medium reviews exact candidate identity/provenance, dependency delta and representative consumer evidence. Required existing remote checks pass. No repeat full OPFS matrix solely for a dependency/version edit; storage changes would require proportional additional gates and an explicit scope amendment.
4. After merge and main/SHA recheck, root dispatches existing publish once. On ambiguous publication, use verify, never blindly republish. Record actual registry0.3.6 tarball integrity/SHA256, exact source revision and workflow result. Fresh registry install imports root, /stems and /assets; expected sparse exports exist and adapter resolves exact SDK0.2.3. Signature/attestation verification passes. Candidate pack evidence is not registry artifact identity.

Close adapter issue only after upstream evidence and verified registry delivery are synchronized. Root preserves evidence outside the worktree, then removes it only when clean, pushed and merged. No app or CLI changes are hidden in this adapter closure.


## Delivery limits

App187 and CLI27 dependency adoption are separately reviewed in their existing issues and not included in this repository's changes. The later full-GET installer/native dispatch and final app deployment remain authorized successors, not this release's claim. Use the current existing publish/verify workflow; it does not share engine753's inappropriate verify dry-run. Keep all current OIDC, exact-main, immutable-version and signature guards. The existing packed headless smoke from engine release0d9af85d can exercise the public installed SDK unshifted-compressor predicate without inventing another harness or copying local binaries. No custom release framework, new browser matrix, kernel changes or generated artifact commits. Final requested Astra xhigh simplification remains after the complete functional flow.
