# Install indexed full-response stems into the verified sparse PCM store

Astra medium rechecked this slice against accepted66 and the accepted parser/store/session APIs. Root reconciled all33 numbered adapter specs and closed dependency68 before creating this issue. Baseline02b1aec321b41ec739e5b25484f3c6f7a311059b includes the completed public adapter0.3.6 / Engine SDK0.2.3 release; its package artifact remains bound to source441efbd015249941b513b9e13f79d8b24b5dca5c. Existing release identity and Effect4.0.0-rc.112 stay fixed in this issue. This brief supersedes the acquisition umbrella's generalized service/runtime and owner-pin prescriptions for this slice. Luna xhigh implements, a fresh Astra medium adversarially reviews; final Astra xhigh simplification follows the full functional flow. Maximum five coherent attempts, one verdict each. Root owns exact-path checkpoints, prompt pushes and GitHub synchronization.

## Smallest closable capability

A caller supplies canonical source identity/shape and a URL/Request locator. One successful cold install reads one 200 full GET containing MISOSTM1, sequentially decodes its packed native FLAC chunks through the existing controller, maps packed frames to timeline spans, and lets VerifiedSparsePcmStore commit only after transport/chunk/full-canonical verification. Warm verified open invokes neither locator nor decoder. Reuse accepted session/pump/storage APIs; no new install coordinator, cache format, session controller or dense fallback.

Preparation downloads every byte of each required asset before it can be ready. All unique sources must complete through accepted openSession/openSparseEngineWebSession before session readiness. This issue provides an indexed acquisition primitive, not a durable offline-library claim: app shell/session snapshots, deletion lifetime ownership, offline presentation and final deployment remain separate. Decode happens during preparation only; warm playback reads the existing sparse PCM cache.

## Concrete public boundary

Export from /stems only:

```ts
export type SparseStemLocator = (
  identity: StemIdentity,
  options: { readonly signal: AbortSignal },
) => string | URL | Request | Promise<string | URL | Request>;

export interface SparseStemDeliveryOptions extends Pick<FlacDeliveryOptions,
  "fetch" | "readDeadlineMs" | "decodeNoProgressMs" | "admission" |
  "memoryBudgetBytes" | "maximumWorkers" | "hardwareConcurrency" |
  "deviceMemory" | "assets" | "createWorker"
> {
  readonly locate: SparseStemLocator;
}

export function createSparseStemResolver(
  options: SparseStemDeliveryOptions,
): (expected: SparsePcmExpectation, signal: AbortSignal) => Promise<SparsePcmResolved>;
```

No Range attempt, maximumAttempts or processing/hash-trust option is exposed by this new API. One call is one attempt; caller retry means a new complete operation. Reuse current option validation/defaults and snapshot worker/asset hooks before use. No worker or request starts at factory creation. Public failures retain EngineWebAdapterError codes/details and complete causal information.

The returned function plugs directly into `SparsePcmSessionOptions.resolve` and `SparseEngineWebSessionOptions.resolver`. Standalone install uses `store.installSource(expected, {resolve: signal => resolve(expected, signal)})`. It returns the existing `{spans: AsyncIterable<SparsePcmSpan>}`; omit the optional index in this first slice because the store derives it and compares the full canonical hash. The manifest remains authoritative only for transport mapping after exact binding to expected; it cannot replace expected identity/shape. No new span/source descriptor types or public generic byte-source/worker framework.

Acquisition is lazy at first iterator pull, allowing return/cancellation before first pull without opening resources. The single-consumer span iterator owns one scoped acquisition through EOF/return/error and releases borrowed blocks when the next pull confirms consumption, or during return/error. Store's accepted iterator ownership already waits for physical settlement. An already aborted signal refuses before locator/worker work.

## One HTTP cursor and finite chunk sources

Use Effect4.0.0-rc.112 HttpClient/FetchHttpClient with one response scope alive through actual EOF or cancellation. Existing fetch override is an actual transport dependency; no fake Range/Response implementation. Normalize locator Request to bodyless GET, preserve established caller credentials/cache/referrer policy, replace its signal with the composed operation signal, and remove Range and If-Range under this documented full-GET contract. Require200 and a body; reject206 and other statuses before decode. No HEAD or preliminary GET, no per-chunk requests, no body retry/resume, no tee/background body collection.

Acquire existing bounded download admission once for the entire response, including time waiting for sequential chunks, output and store backpressure. Do not release/reacquire it per chunk or use the same single permit recursively for decoder admission. Share the existing pool/admission implementation across calls to one resolver, with at most one active worker lease per source and one fresh worker per chunk. Workers terminate before the next chunk lease; no reset/reinitialize optimization.

Content-Length is optional. An exposed value must be a valid nonnegative safe integer matching the exact admitted header+manifest+payload length. Missing/inaccessible length is allowed; actual byte counts and final EOF are authoritative. Accept absent or visible identity Content-Encoding; reject any visible other encoding before decoding. Browser header visibility is a caller/CDN representation contract, not proof that an inaccessible header is absent. Do not attempt to set forbidden Accept-Encoding or compare encoded transfer length to decoded representation length. No CDN change is required; public CDN observations are contextual evidence, not a runtime prerequisite.

Preserve the documented Safari/iOS16.4+, Chromium102+ and Firefox111+ support floors. Choose the reader once on the same actual response: use getReader({mode:"byob"}) where supported; if acquisition is unsupported and the body remains unlocked, acquire its default reader. A selected reader's subsequent failure is terminal, never a reason to try another reader or request. Do not use user-agent sniffing, synthetic byte-stream wrapping, background collection or another HTTP request.

The adapter creates only fixed-size transport/decoder working buffers and retains at most one browser-delivered input view per admitted response. BYOB-capable bodies constrain that view and backing to256KiB. For default-reader compatibility bodies, the browser chooses the delivered backing size; it is accounted separately and is not covered by the fixed worker reservation. This held backing is not opaque browser-internal memory, and its byte size has no portable duration-independent ceiling. The adapter neither collects nor intentionally buffers the complete source. Preserve existing concurrent-response admission; actual held default backing totals are the sum of those live backings. Do not claim unconditional mobile memory safety from these bounds.

For BYOB, submit one<=256KiB backing and treat it as transferred immediately; reuse only the returned backing after all carry bytes and source borrows are consumed. Short positive reads are progress. Process nonempty returned bytes even when done is true; cancellation's undefined terminal value is not successful EOF. For default reading, retain one original Uint8Array and its actual buffer.byteLength, consume bounded slices before another read, and never copy or queue the whole delivered view. A small view can retain a larger backing; neither arbitrary1MiB rejection nor slicing proves a bound on it. Both paths share the cursor/source ownership, finite extent/hash accounting and physical cleanup. An optional separate<=256KiB scratch is allowed if it simplifies ownership. Metadata gets its separate<=8MiB manifest allocation after header preflight; do not assemble payload/chunk ArrayBuffers or Blobs.

This policy corrects the disproved1MiB assumption without increasing the existing browser minimum. Actual Chromium delivered a valid2MiB backing under backpressure; current Chromium/Firefox/WebKit BYOB probes pass, but these desktop observations do not qualify old Safari or physical mobile playback. Fetch byte-stream compatibility specifically differs from general Response.body support: [MDN compatibility data](https://github.com/mdn/browser-compat-data/blob/main/api/Response.json), [default reader contract](https://streams.spec.whatwg.org/#default-reader-read), [BYOB ownership](https://developer.mozilla.org/en-US/docs/Web/API/ReadableStreamBYOBReader/read).

Read exactly16 bytes and use `admitSparseStemHeader`; then read exactly admitted manifestBytes and use `admitSparseStemManifest` plus `assertSparseStemSessionBinding(manifest, expected)`. Reuse all accepted interval/chunk/object caps: <=65536 records per array, <=8MiB exact canonical manifest, <=32MiB encoded and <=30 seconds packed frames per chunk, <=8GiB object, launch rates and exactly1|2 channels/16|24 bits. Counts and lengths are checked before growth/Schema traversal. Byte cursor offsets are checked safe integers. No second manifest validator.

Preserve accepted eight-byte magic exactly: ASCII `MISOSTM1`, hex `4d 49 53 4f 53 54 4d 31`. Existing header admission validates all eight bytes. No format correction is needed. Unknown/truncated magic and native fLaC refuse here without parser fallback. Successor C adds positive native dispatch from the same GET prefix.

Each chunk supplies a private DecoderByteSource over only its declared encoded extent. Its actual accepted66 failure type is DecoderByteSourceError, not EngineWebAdapterError; preserve the tagged prepare/read/finish operation and original cause. Accepted66 passes `DecoderByteSourceOptions.borrow = { adopt(release), release() }` into the source factory. Real ranged delivery adopts ownership before acquisition/progress callbacks; the new finite cursor must likewise adopt any source-visible borrowed bytes synchronously before a yield, callback or abort can discard their return value. Use that one controller-operation owner, already protected by the input scope finalizer; release/clear after metadata use or input-slot copy, and during finish/error/interruption. Do not register one finalizer per historical read, defer adoption to the controller after source.read returns, or permit a new cursor read/scratch overwrite while the current borrow is live. The outer response scope owns its carry view/body reader; the per-decode owner covers the current source-visible lease, not the entire HTTP response. prepare reads STREAMINFO and scans/skips remaining metadata sequentially in bounded slices, hashing all bytes including magic/metadata; retain <=128 metadata-block bound and existing decoder block restrictions. Expected sample count/PCM byte count are CHUNK values, while rate/channels/depth match the full source. read obeys existing input credits <=256KiB and never crosses into the next chunk. finish drains/hashes the finite unread suffix without waiting for another worker credit, then verifies exact extent and flacSha256. Cursor ownership serializes prepare/read/finish; no concurrent body reads.

## Minimal private controller adaptation

Use accepted66's single input lane/controller. Its current `createFlacStemResolverWithSource(options: FlacDeliveryOptions, sourceFactory)` is still a test-only module export, absent from package entrypoints; `makeFlacStemResolver` still requires locate and constructs range delivery. It is not yet a ready production full-response entry. Extract/factor a narrow private invocation of that same controller accepting a source factory with borrow ownership, parent identity, chunk decode context and shared pool resources. Keep range-specific options/construction in the legacy wrapper, so B supplies neither a dummy locate/range callback nor a fake response. Do not expose DecoderByteSourceOptions.range as a requirement of the finite source. Preserve legacy createFlacStemResolver API and its exact range behavior. Do not copy the controller or expose source internals at package entrypoints.

The existing public ReadableStream path currently sends output-credit on enqueue. B needs a PRIVATE borrowed-output path through that SAME queue: `{bytes, release}` where release sends credit exactly once after all mapped slices are consumed; cancellation releases/discards ownership without a late worker message. Legacy ReadableStream adapter can preserve its existing enqueue semantics. Do not obtain B output by eagerly draining/copying that public stream. Keep two <=384KiB worker output credits, one256KiB input slot and existing2MiB decoder memory. Account for the original retained output buffer even when its views are <=128KiB. This is a real bounded controller change: accepted66's PCM admission and watchdog currently inspect only `blocks.length`, and its stream shifts a buffer then immediately credits it. In the private path the maximum-two count must include queued buffers PLUS the one buffer checked out to the span mapper; shifting cannot free a credit or resume the stall timer. Resume only when release actually returns credit. Cancellation/error discards queued and checked-out ownership exactly once, and late release after terminal worker settlement must not post a credit. Preserve the same queue, single input command lane and physical worker lifecycle; no parallel output controller or public stream drain. A small shared take/release primitive with a legacy stream wrapper and a private consumer is sufficient; if implementing it requires a wholesale controller rewrite, stop and rescope before layering acquisition on top.

Pass whole-source identity as diagnostics/context and chunk shape plus expected chunk PCM digest as distinct private decode context. Do not supply a chunk digest as a fake whole-source identity, compare chunk output to the whole source digest, or register a chunk as a verified canonical source. Always hash chunk packed PCM in the host with IncrementalSha256 for this slice, including with injected workers/assets. Worker digest is not trust authority. Enforce exact PCM bytes/frames and frame alignment regardless of optional legacy processing behavior. In particular, accepted66 currently enforces prepared totals only when options.processing exists; B must have an explicit private strict decode policy, independent of processing, and validate each PCM block frame count against its byte length as well as final chunk totals. Worker start keeps the parent identity, uses chunk shape, and disables whole-source worker-digest authority; host chunk digest verification remains mandatory.

Map packed frame offsets monotonically through admitted intervals. Split each borrowed block at interval boundaries AND largest frame-aligned size <=128KiB (stereo24 requires rounding). Each emitted `{startFrame,bytes}` is a view into the bounded original block; never materialize omitted gaps. One chunk can span several intervals and several chunks can occupy one interval. Advance exactly once and release the original output only after its final slice. Full packed endpoint must match interval/chunk totals.

## Completion, cancellation and Effect scope

Span done requires every chunk decoded with exact shape/count/host packed-PCM hash, each finite encoded extent/hash finished, mapped packed totals exact, and actual object EOF after the declared endpoint. A correct Content-Length never replaces EOF. All-silent manifests use zero workers but still read/admit the entire manifest and require object EOF. Store then hashes implicit gaps/final gap and active bytes against authoritative whole-source identity, closes data and publishes marker last. No sourceComplete/progress success or returned verified descriptor precedes the store's complete canonical verification.

Decoder frame completion does not report exact compressed parser-consumed position. Draining validates finite bytes/hash; it cannot prove semantic consumption of an intentionally declared, rehashed suffix. State this limitation plainly; no codec ABI expansion. Undeclared trailing bytes, truncation, or changed declared suffix without matching manifest hash must fail before span EOF.

Apply pinned effect-ts skill at /data/sparse-pcm-launch/tooling/unconfirmedlabs-skills/effect-ts/SKILL.md (upstream5d24b996b95511183e3f9da6b9aeda8eac3aa9c8). Compose actual HTTP/worker/iterator ownership with named Effect.fn/gen, scoped acquire/release, bounded pull-based Streams, Schema.TaggedError operational failures and Clock deadlines. Validate new option/header boundaries with Schema after hard caps while wrapping accepted pure parsers unchanged. Preserve the full Exit/Cause, including defects/interruption and cleanup failures, at the Promise/iterator edge. Follow accepted66 attempt3: retain original reason/payload objects by reference in shallow wrappers, including explicit undefined and typed source context; never recursively traverse causes/AggregateErrors or add a generic cycle walker. Physical cleanup/quarantine failure keeps its established primary code/details while retaining the operation failure. No nested runPromise in sources, per-credit runtime, new ManagedRuntime, service catalog, generic source registry or install service facade without an actual consumer. Existing store runtime and66 controller edge remain; dependency Layers represent real resources only.

Network deadline applies while a body read is pending; downstream store/credit backpressure must not expire decoder progress. Abort/return/error stops new input, interrupts actual HTTP, terminates active worker through existing pool ownership, settles source/iterator finalizers and releases admission. Store cleanup runs only after physical resource settlement. Terminal completion cannot enqueue late input or mutate a released borrow. Failure leaves no newly accepted cache marker; previously committed sources survive.

## Focused gates and file budget

Use synthetic fixtures and the existing test/packed-browser/OPFS machinery. No timing work or private artist data.

- Integrated cold install with actual small FLAC chunks covers mono16/stereo24, gaps, one chunk across intervals and multiple chunks in one interval. Reconstruct bounded cache windows versus a dense oracle, require full canonical hash and physical payload bytes equal active PCM. Count one GET, no Range/HEAD, sequential worker leases and no whole-object/chunk buffers. Warm reopen/install must call locator/fetch/worker zero times.
- Segmentation table splits header, manifest and metadata; absent Content-Length succeeds. Wrong/malformed exposed length, unsupported visible encoding,206, each-section truncation and trailing object byte refuse. Positive default-reader2MiB and small-view/larger-backing fixtures must consume bounded slices with only one backing held; real byte-stream BYOB fixtures assert supplied/returned backing<=256KiB and no reuse before release. Unsupported BYOB acquisition chooses default once on the same response; a later BYOB read failure never retries. Cancel/error/quota and pending-read settlement gates exercise both modes. Reuse accepted parser malformed/canonical/count gates instead of duplicating their corpus.
- Delayed encoded finish/EOF blocks span done and commit; mutate final compressed bytes after PCM completion, reject chunk PCM hash/shape/count mismatch, and reject final full-source hash mismatch. Custom worker digest cannot bypass host checks. All-silent success and trailing-byte failure both instantiate zero workers.
- Hold a >128KiB borrowed stereo24 block across multiple slices/gaps and stall the store: output credit stays withheld until final slice consumption, bounds remain fixed and cancellation settles it once. Focused cancellation at HTTP wait, worker admission/read/finish and pending store consumption plus injected quota failure proves no committed marker and admission/resource settlement. Reuse66 stale-credit/no-progress tests with only integration-specific additions.
- Run focused new/changed tests, typecheck/format/source policy and one full package check. Extend the existing packed Chromium fixture for one REAL packaged decoder -> indexed GET -> real OPFS cold/warm install, retaining the real-worker request/count/hash evidence. Do not substitute an injected descriptor map for this acquisition proof or add another harness/browser matrix.

Expected source: new src/stems/sparse-resolver.ts and one private full-response cursor module; narrow flac-resolver.ts/decoder-byte-source.ts production borrow/context seam; src/stems/index.ts exports. Touch flac-delivery.ts only for a small shared request-policy helper if needed. Tests: one focused sparse acquisition file, existing resolver tests where seam changes, existing packed-browser fixture, sparse contract docs and issue evidence. No new session/store/pump orchestration, worker protocol or decoder-C changes.

Main risks are actual Effect response lifetime, delayed encoded drain versus worker completion, output-credit timing, and extra manifest/network metadata accounting outside historical worker reservation. Record bounded manifest byte/text/parsed-index overhead separately; do not claim the old8MiB worker reservation covers up to8MiB encoded JSON plus its decoded structures. Enforce the concrete per-operation caps and existing admitted concurrent-body width. If this needs a second controller or broader subsystem, stop/rescope before implementing it.

## Successor and delivery boundary

C adds positively identified native fLaC full GET, exact integer zero-run extraction and the SAME sparse store/controller/cursor; it does not route through dense persistence. Final dispatch replays one same-response prefix, selects exactly indexed or native once, rejects unknown/truncated magic and never retries a failed parser as the other format. Existing legacy native range API may remain unchanged. App/lifetime/snapshot/deployment and any broader qualification are separate issues. No owner/pin files, offline snapshot changes, engine DSP, SDK release or CDN configuration belongs here.

Dependency issue68 owns exact registry SDK0.2.3 adoption and adapter0.3.6 publication before B starts. B consumes that synchronized release baseline; no SDK/package version, release preparation or registry publication is added to this installer issue. Root checkpoints the first focused-green exact-path tranche before further edits, completes representative gates and fresh review, pushes required evidence, synchronizes/closes the matching GitHub issue and cleans the completed worktree only after merged delivery and preserved evidence. Number/title must be reconciled against GitHub before implementation.


## Smallest implementation checkpoint strategy after68

1. Root numbers/synchronizes the accepted B brief after68 closure. Luna's first coherent tranche adds the narrow same-controller borrowed-output/strict-chunk seam, with focused legacy and private-source ownership/count/hash/finish discriminators. No HTTP installer or second controller is layered on until root commits that focused-green exact-path tranche. This checkpoint materially establishes the missing installer prerequisite; it is not B acceptance.
2. Add the single scoped full-response cursor and finite source, span mapper and public resolver directly into verified store installation. Use one real small indexed FLAC cold/warm case as the first vertical proof; root checkpoints it once focused-green. Keep the bounded malformed/abort/EOF table and existing real packed Chromium extension in this same issue, without creating another harness.
3. Freeze the coherent candidate, run proportional focused and package/packed gates once, then receive one fresh Astra medium adversarial verdict for the attempt. Subsequent corrections follow the bounded attempt rule, with exact-path checkpoints. B closes only after verified store installation and representative cancellation/integrity evidence are upstream and GitHub synchronized. Native dispatch C and final app-flow simplification remain separate.

## Attempt 1 recoverable private-controller checkpoint

Luna added private chunk shape/host hash checks and a borrowed output path to the existing controller, plus three focused tests. Producer reports those three tests, existing baseline278 tests, typecheck, formatting and lint pass; these are separate observations, not a claimed full281-test frozen qualification. No HTTP cursor/installer or public export was added. Root preserves this buildable tranche before further work.

Root inspection identifies unfinished frozen requirements: the private factory casts an undefined locate into the required legacy options and casts a borrowed-only result as ResolvedStem; range-specific construction/types must remain in the legacy wrapper instead. Private pull can overwrite the sole checked-out owner if called again before release, undercounting retained buffers; enforce one live checked-out borrow and exactly-once cleanup even on enqueue failure. Add the already-required wrong host-hash, strict chunk-total and delayed-finish discriminators. Preserve legacy behavior and source/Cause ownership. These corrections stay in the SAME controller/queue and must be checkpointed before layering HTTP acquisition. This is still attempt1, not a verdict or permission to broaden into a new controller/framework.


## Attempt 1 private-controller correction checkpoint

Luna completed honest discriminated legacy/private resolver return paths, removing the dummy locate/result cast, dummy stream and global output side table. The finite source factory has no required ranged callback. Root further limited range closure construction to a real locator and restricted the new zero-length-block refusal to the explicit private chunk policy, preserving the legacy admission contract. One checked-out original PCM owner blocks another private pull until release; cancellation/error and enqueue failure release the actual owner, and late release never returns worker credit. Wrong host hash, strict final totals, delayed source finish, repeated pull and cancellation discriminators were added.

Root reran build, test compilation, all41 focused resolver tests, formatting and lint/typecheck against the corrected working tree: all pass. Exact retained logs are `/data/sparse-pcm-launch/tooling/adapter-71/attempt1/correction/root-*.log`; producer logs are alongside. This is a recoverable focused-green prerequisite checkpoint within attempt1, not the independent verdict or full installer acceptance. The older278-test observation is historical; no current complete-package total is inferred. No HTTP installer or public export is present yet. The next tranche is the frozen single-response cursor/finite source/span mapper plus real verified-store cold/warm proof.


## Attempt 1 incomplete HTTP recovery checkpoint

Root paused implementation after the private-controller checkpoint's thirty-minute recovery boundary. The draft adds one response cursor, finite source, lazy span mapper and public resolver plus an injected-worker test. Root's fresh build passes; the producer reports typecheck passing. The focused test hangs before its first completed subtest and timed out; no cold/warm install, real decoder, cancellation, format/source-policy or full-package acceptance is claimed. Stale producer test processes were terminated. Retained root build log: `/data/sparse-pcm-launch/tooling/adapter-71/attempt1/http-recovery/root-build.log`. This checkpoint preserves useful buildable work within attempt1; it is not a review verdict.

Known incomplete work: response cleanup currently waits for a pending read before cancelling it, which can deadlock; its promise-clear comparison uses the wrong promise identity. The synthetic worker's unbounded microtask polling can starve scheduler progress. The mapper needs its per-chunk frame counter advanced, and the required real multi-block FLAC proof is absent. These must be corrected before qualification. Ownership, full Cause preservation and caller Request cancellation remain subject to the frozen gates, not inferred from successful compilation.

The original1MiB default-reader cap is on hold: an actual Chromium normal-CDN full response delivered a valid2097152-byte backing under downstream backpressure. Actual current Chromium/Firefox/WebKit Fetch BYOB supports a supplied256KiB buffer; a complete stalled Chromium BYOB read matched all26962575 bytes and the object SHA with max262144-byte backing. These are transport probes, not decoder/OPFS/mobile qualification. Official Fetch-specific compatibility is Safari26.4+/Chrome116+, conflicting with existing documented Safari16.4+/Chrome102+ session support. Root has asked the user whether to retain those floors with one honestly browser-sized default-reader backing and BYOB where supported, or require newer browsers for a strict transport-buffer bound. No support increase, arbitrary raised cap or weaker memory claim is adopted pending that ruling. The draft's old cap is not a finalized policy. Pure mapper/source and modern BYOB work can proceed independently.


## Root reader-policy ruling after the measured counterexample

Root selects the compatible policy above within the already authorized implementation scope, preserving the existing browser floors. The optional user preference was not answered; this is an explicitly communicated implementation assumption, not an approval or inferred answer. The1MiB cap originated in our brief and is withdrawn based on actual valid browser delivery. The amended contract explicitly gives up a fixed total held-default-buffer byte ceiling; it preserves fixed package-created buffers, one live browser view per response and the existing decode/store/worker bounds. Astra medium's concrete analysis is `/tmp/adapter71-reader-policy-options.md`; no new controller or cache architecture is introduced. Current full stalled Chromium BYOB proof is `/data/sparse-pcm-launch/tooling/artist-publication/browser-byob-stalled-full-probe.json`: exact26962575 bytes/SHA,367 reads, maximum262144-byte view/backing. Earlier recovery notes record the then-pending decision historically; this ruling supersedes that hold.


## Attempt 1 first default-body installation checkpoint

The scoped cursor, finite source and mapper now complete an injected-worker cold install from a real encoded FLAC fixture through VerifiedSparsePcmStore, with one default-reader GET and a warm reopen requiring no locator/fetch/worker. A caller Request abort test also passes. The former microtask-starvation hang, pending-read cleanup ordering/promise identity and missing mapper frame counter are corrected. Cursor publication now synchronously adopts the source borrow; pending concurrent pulls are refused. The reader selects BYOB/default once under the amended compatibility policy.

Producer build/typecheck/format/lint and2focused tests pass. Root rebuilt, compiled tests and ran43 combined legacy/private resolver plus new installer tests: all pass. Exact logs are `/data/sparse-pcm-launch/tooling/adapter-71/attempt1/installation/{root-build.log,root-test-compile.log,root-focused.log}` with producer logs alongside. This is an incomplete buildable checkpoint, not a full installer PASS. The injected worker does not qualify real FLAC decoding.

Root independently found a remaining ordinary BYOB EOF failure using an all-silent valid indexed source and Node's actual byte-stream Response: installation fails with stem.delivery.range because a normal done:true empty Uint8Array is rejected as a zero-length data chunk. Retained executable reproduction and output are `installation/root-byte-body-repro.mjs` and `.log`. This must pass before the real-browser gate; canceled/undefined BYOB terminal and done:true nonempty carry handling also remain mandatory. Do not treat the43 passing default/legacy tests as BYOB qualification. Genuine multi-block packaged libFLAC/OPFS cold/warm, body ownership/terminal cases, malformed/final-EOF/full-Cause cancellation and complete package checks remain outstanding before the fresh independent verdict. Root supplied one72000-frame stereo24 synthetic fixture (432000PCMbytes,281039FLACbytes) with independent reference-decode equality under `attempt1/multiblock-fixture/`; no artist audio is used.


The ordinary BYOB EOF correction now passes the retained root reproduction (oneGET, zero active bytes, no decoder) and an added real byte-Response test. Carry is consumed before terminal EOF, empty final BYOB views succeed, nonempty final views remain consumable, undefined BYOB termination refuses, and default-reader undefined EOF remains supported. Producer3focused tests/build/test compilation/typecheck/format/lint pass; logs are `installation/byob-correction/` under the retained71 evidence. This narrow correction is checkpointed before the actual multi-block packaged decoder/browser/OPFS integration and remaining frozen corpus; it is not the final71verdict.


## Attempt 1 first real packaged decoder installation probe

The existing packed Chromium harness now runs an indexed fixture through actual packaged libFLAC workers and OPFS. Retained log `installation/real-vertical/browser-indexed.log` reports one fullGET, two36000-frame FLAC chunks,432000 activePCM bytes,17 mapped spans across chunk/interval boundaries,600000 canonical timeline bytes with full identity verification, and warm verification with zero additional locator/network/decoder calls. Worker errors are empty. Root formatcheck passes; npm pack's build and the harness's fresh consumer typecheck/Vite build passed inside that run.

This is a recoverable real-decoder probe, not final packaging/installer acceptance. The first instrumentation overrides worker construction and the test server serves package-relative module dependencies outside the Vite output; restore default package Worker construction with observer-only counting and remove that test-only serving workaround before claiming ordinary consumer deployment. Both current chunks decode only216000 bytes, so the supplied72000-frame/432000-byte single-chunk fixture must be used to exercise the intended output/input-credit crossings. Keep this same harness and one synthetic fixture, not a new framework. Remaining integrity/ownership corpus, full package gates and fresh independent verdict are still required.


## Attempt 1 ordinary packed consumer and physical ownership checkpoint

The same indexed packed harness now uses default package Worker construction, observer-only counting, and ordinary Vite-built assets; the extra source-module serving routes and asset/worker overrides are removed. A frozen synthetic72000-frame stereo24 chunk is281039 encoded bytes and432000 PCM bytes, crossing the input/output-credit sizes. Two such chunks map across intervals in oneGET. Actual browser result: twoFLAC workers, oneOPFS worker,33spans,864000 active bytes and verified984000-byte/164000-frame canonical timeline; warm reopen has no additional locator/network/decoder calls and workerErrors is empty. The repository fixture and its independent reference-decode/hash provenance are documented under tests/fixtures.

Root identified the unresolved-resolution interruption gap; source ownership now retains late returned output for cancellation until physical promise settlement. Iterator close retains complete close/interruption Exits rather than only the squashed error. The stale1MiB body-view diagnostic constant is removed under the accepted reader policy. Producer build, typecheck, test compilation,44 combined focused tests, format and lint pass. Exact logs: `/data/sparse-pcm-launch/tooling/adapter-71/attempt1/final-qualification/`. These are a coherent recoverable checkpoint and real ordinary-consumer proof, not the final71verdict. Remaining frozen malformed/terminal/ownership integration discriminators and whole-package qualification precede independent review.


Root's complete `npm run check` passes at62c4282: format, typecheck/source policy, decoder binary policy, all287 tests, and packed package policy (190files,230645bytes). Log `attempt1/final-qualification/root-check.log` is retained. The ordinary packed browser result above covers the same source. The remaining frozen integration failure/ownership discriminators still need to be added; this full green observation is not an independent acceptance verdict or permission to omit them.


## Attempt 1 candidate submitted for independent review

The new integration table now covers header/manifest/metadata truncation, malformed or wrong visible Content-Length,206/nonidentity encoding, trailing bytes, absent length with a2MiB default backing, BYOB terminal nonempty/undefined cases, terminal BYOB failure without reader retry, and changed final encoded bytes after PCM output. New orchestration functions use named Effect.fn; the ordinary browser harness publishes its success only after store/backend cleanup. Producer has stopped the source tree for root checkpoint and fresh Astra medium review.

Full `npm run check` passes293tests plus format/lint/typecheck/source/decoder/package guards (190files,230754bytes). Log `attempt1/final-qualification/check-final.log`; ordinary packaged browser `browser-final.log` repeats oneGET/two72000-frame chunks/864000 active bytes/33spans/984000 canonical bytes and warm zeroadditionalacquisition with no worker errors. The reviewer must independently map the complete frozen failure/ownership/resource gates to evidence; this submission is not itself a claim that every such gate is discharged. In particular integrated store failure/cancellation, delayedEOF, bounded borrowed credit and complete causal retention require adversarial assessment beyond the happy path. Maxfive attempts remains.

## Attempt 1 independent FAIL and bounded attempt 2

Fresh Astra medium reviewed frozen candidate7c2e512934b017fc9848da01c9857874ae1c34f3 and independently passed50 focused tests. Report and executable discriminators are retained at `/data/sparse-pcm-launch/tooling/adapter-71/review-attempt1.md` and its sibling `review-attempt1/` directory. The single consolidated verdict is FAIL. Producer package/browser evidence remains valid for the tested happy path; it does not establish the following ownership requirements.

1. The executing Effect HTTP client replaces the intermediate Request signal. Resolver and caller-Request cancellation before headers do not abort the actual fetch, pending locator work lacks an operation-abort race, iterator return can finish before physical fetch settlement, and late/refused Response bodies are not cancelled. Retain the actual fetch promise and register response ownership before validation; connect cancellation to the executing locator/header Effects. Cancel late/rejected bodies and wait for physical settlement before releasing admission. Use the same cursor, scope and real fetch dependency. The accepted legacy delivery contains a local physical-promise/late-body pattern; no second HTTP framework or runtime is needed.
2. Operation failure currently wins over an explicit physical cleanup failure, losing cleanup's primary EngineWebAdapterError code/details. Keep cleanup primary while retaining operation and cleanup Causes/payload references with shallow known wrappers and full Exits. Ordinary operation failure remains primary when cleanup succeeds. Do not recursively search error graphs or squash resource Causes at the edge. Installed Fiber.interrupt returns void; obtain Fiber.await explicitly if the interrupted fiber Exit is needed.
3. Validate locator and both deadlines once at factory construction with the pinned Schema boundary and existing defaults/ranges. Invalid options must fail even for all-silent input, before starting workers/network. Retain snapshots of asset URLs and worker hooks; add a compact mutation discriminator rather than redesigning the options layer.

Attempt2 is one coherent correction of these three groups plus proportional existing-seam integration proof: original >128KiB stereo24 output held across mapper slices and a gap during a deferred store write, no credit before final slice consumption and no backpressure timeout, cancellation/quota failure with physical settlement before cleanup/no new marker/preservation of committed data/safe resource reuse. Cover default and BYOB pending reads without multiplying the full matrix. Independent delayed-actual-EOF commit and whole-canonical-hash rejection probes already PASS; preserve equivalent regressions. Reuse the current controller, store, fixture and packed harness. No native dispatch, cache format, session coordinator, release, benchmark or browser matrix belongs in this attempt.

A custom reader cancel promise that never settles may keep return pending. The read deadline governs pending network reads, not physical cleanup; truthful waiting is acceptable and is not a request for a new timeout/quarantine policy. Unsafe early release is the defect. Luna xhigh implements this bounded correction, checkpoints coherent focused-green work, then a fresh Astra medium supplies the next single verdict. Maximum five attempts remains unchanged.


## Attempt 2 focused ownership correction checkpoint

Luna stopped a focused-green correction of the existing response/resolver and tests. Actual locator/fetch promises remain owned until settlement; abort reaches the executing HTTP operation, late/refused bodies are cancelled, and expected fetch abort rejection does not become a fabricated cleanup failure. Explicit cleanup code/details remain primary through shallow known Cause wrappers. Factory options and asset values are detached once and then validated, including locator/deadlines, without starting resources.

Producer18 sparse tests PASS, plus build/test compilation/typecheck/format/lint/source checks. Logs `/data/sparse-pcm-launch/tooling/adapter-71/attempt2/{correction,installation}/`. The retained independent attempt1 executable now demonstrates correct pending-header abort, delayed physical return, late/status body cleanup, invalid-option rejection and cleanup-primary behavior; delayed actual EOF and final canonical hash probes still pass. An added72000-frame stereo24 fixture/injected-worker case holds a >128KiB original output across mapped slices and a timeline gap during a deferred store write, then installs successfully. This is a focused ownership checkpoint, not real-decoder requalification or the second verdict.

Remaining frozen attempt2 evidence: integrated store quota/cancellation and no-new-marker/preservation/safe-reuse settlement checks, representative pending body-read cancellation in default and BYOB modes, and retained delayed-EOF/full-canonical regressions. Complete these using the same existing seams, then freeze source and run the proportional complete package and ordinary packed browser gates before fresh Astra medium review. No broader matrix or new framework is added.


Root inspected the first deferred-store fixture after its checkpoint: its injected worker emits complete immediately after its two PCM blocks, so the zero-credit assertion is also explained by terminal late-credit suppression. It proves successful mapping through a delayed write, not nonterminal credit timing. The final attempt2 tranche must use the same fixture with at least a third bounded output gated on an actual returned credit, hold mapper writes across the first original's gap/slices, and demonstrate no early credit followed by the credit required for completion. This corrects evidence scope without changing the product contract or adding a new framework. Existing no-progress exemption must also be exercised while two original credits are retained.


## Attempt 2 three-output and store-failure checkpoint

The same injected worker now has three bounded PCM outputs and cannot publish its third output/complete until the first original earns a returned credit. The deferred-store test observes no early credit, survives a hold longer than the configured5ms decode watchdog while two originals are retained, then requires one credit for completion. Added tests exercise pending BYOB cancellation and an all-silent quota failure preserving earlier committed files followed by same-resolver reuse. These are20 passing sparse tests; full `npm run check` passes304tests and all format/lint/type/source/decoder/package guards (190files,232812bytes). Logs `attempt2/final/` retain the run.

The first browser rerun did not find an executable through automatic discovery; Chromium is installed at the existing qualification path and must be selected with CHROME_EXECUTABLE. No browser playback failure was observed, and no current browser PASS is claimed yet. Root checkpoints this coherent source before the ordinary package rerun and remaining active-worker store-failure/physical-settlement assessment. The all-silent quota test alone does not qualify cleanup while a decoder is active. This remains attempt2 before its fresh verdict.


## Attempt 2 ordinary packed-browser result

Root reran the existing packed-consumer indexed profile at `dba1436` with the installed Chrome-for-Testing executable. It passes through the package's ordinary Worker and OPFS paths: one full GET, two 72,000-frame FLAC chunks, two FLAC workers, one OPFS worker, 864,000 active PCM bytes mapped into 33 spans, and exact verification of the 984,000-byte / 164,000-frame canonical timeline. Warm reopen adds no locator, network, or decoder work and reports no worker errors. Result: `/data/sparse-pcm-launch/tooling/adapter-71/attempt2/final/browser/result.json`. This completes the requested single browser happy-path rerun without adding a browser matrix or new harness. The candidate now proceeds to its one fresh independent attempt-2 verdict.


## Attempt 2 independent PASS

Fresh Astra medium records PASS for frozen candidate `2800ac9`. The independent run passes 20 sparse-acquisition tests, 41 existing controller tests, retained attempt-1 ownership discriminators, and four focused active-worker/store probes. Quota failure and cancellation during an active decode wait for physical store/reader settlement, terminate the worker, preserve an earlier committed source, and permit safe resolver reuse. Report and runnable evidence: `/data/sparse-pcm-launch/tooling/adapter-71/review-attempt2.md` and its sibling evidence directory. This verdict covers the indexed audio-ingestion capability only; it adds no app, offline-shell, UI, native-dispatch, benchmark, or framework claim.
