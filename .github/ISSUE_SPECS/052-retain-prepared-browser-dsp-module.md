# Retain prepared browser DSP module through verified session opening

Base: adapter 6dbc3c8f9f0f9241780799d744c42ebecadb0b7c (unpublished 0.3.5). Upstream capability is specified in misofm/engine#719 on engine base 0df770b0f1f002b563246db70b71a3991fe8a8c7. The user authorizes continued Astra xhigh scalable 64-track work and cold-start iteration. No engine source is copied into adapter runtime.

## Problem and behavior

Shape-only scratch boot releases its compiled module before live engine creation, allowing first-use DSP compilation on the audio thread. The upstream engine adds a bounded worker preparation API returning validated shape plus WebAssembly.Module and an optional preparedModule host handoff. The adapter default must retain that exact result through complete canonical source verification and use it in live creation. No real source becomes ready during synthetic preparation; download concurrency, storage verification, shared runway, bounded pump memory and realtime PCM-only behavior remain unchanged.

Default opening calls prepareBrowserSessionWithWorker once with the owned document and policy snapshot, retains its module while storage ingests/verifies every source, then supplies preparedModule to createEngine and its default/custom host. Existing explicitly supplied scratchBoot remains a shape-only compatible escape hatch and omits preparedModule. Preserve deployment URL/worker factory overrides, abort causes, typed deadline/capability errors and exactly-once worker termination before storage begins. Do not add a user-visible preparation stage.

## Objective gates

Session tests prove default preparation once, physical worker termination before storage, no context/live host before verified lease, local module identity through delayed verification and host handoff, matching owned document/policy snapshots despite caller mutation, and continued custom scratch compatibility. Worker wrapper tests retain URL/factory forwarding, deadline translation, abort and clone failures. Run full adapter check and fresh packed healthy/terminal worker gates against the reviewed exact upstream engine archive. Final actual 64-track app graph qualification uses current unbatched writes, cold/warm sustained EOF crossing and repeated seeks, full independent source digests, HTTP concurrency four, and high-resolution full callback traces as specified by engine719.

Root owns reviewed engine-to-adapter-to-app version pins, main merge, publication and integration. No publication in this implementation tranche. Preserve all earlier immutable research and archives, including failed rehearsal variants and the 2.679ms instrumented warm trace residual.

## Status

Spec created before implementation. Engine719 feed cap checkpoint b69a01ab passes13 focused PCM tests; additive prepared-module API is being implemented and independently reviewed. Adapter191 tests and prior packed healthy/terminal gates pass at the base revision. New default consumption and exact packed qualification remain pending.


Default adoption now uses the SDK preparation result once, keeps its compiled module alive through the entire verified storage await, and hands it to createEngine/default or custom live hosts. Explicit custom scratchBoot remains shape-only and omits preparedModule. Shared wrapper translation preserves deployment Worker/Wasm URLs, abort causes and deadline categories. Session tests prove termination-before-storage and deferred verification-before-host, local received module identity, owned document and nested policy words through caller mutation (with intentionally different physical require_* words), and custom override compatibility. All191 tests, format, type and source-policy checks pass against the isolated current engine candidate build (`/tmp/miso-adapter-prepared-tests.log`). Exact new engine version pin, final package qualification and high-resolution64-track startup traces remain pending; this checkpoint is not publishable against registry0.2.1.

The exact engine0.2.2 candidate is frozen at `/tmp/miso-engine-0.2.2-reviewed/misofm-engine-0.2.2.tgz`, SHA256 `30639cf1f0f9707534020d584736339da3ef08981af5b6c5148f3ac310261e58`, source `cf7e695b29043abcae9f92d5172b719e33764f03`. Adapter dependency, lock integrity, source provenance, README and publication identity checks now name this artifact. The local registry-style lock entry awaits upstream publication; it is not evidence that0.2.2 is already on npm. Full adapter check passes191/191 plus format/types/source/decoder/package policy (`/tmp/miso-adapter-0.3.5-prepared-check-final.log`). The first full run caught the foundation test's intentionally pinned prior engine commit; that assertion was updated to the new exact candidate, then the whole check passed. Independent Astra xhigh review finds no implementation blocker. Fresh packed healthy/fault browser gate and actual64-track production callback qualification remain pending.

## Final qualification and delivery handoff

Frozen adapter source `4e307e3c641a17fd192bd106c8dae7d2ad8f1162`, archive `/tmp/miso-adapter-0.3.5-prepared-reviewed/misofm-engine-web-adapter-0.3.5.tgz`, SHA256 `607c983da2332de7b349b3f7506af37a8de926d47dd158b20ed5bf71481fbace`; all158 payloads match the source/build. Adjacent receipts and browser-result.json retain proof. Fresh packed browser gate passes (`/tmp/miso-adapter-0.3.5-prepared-browser.log`): initial/resumed/running exact first PCM and full contiguous generation runways; actual read rejection,5-second read deadline and worker crash each closes/notifies/terminates once, with no unhandled errors.

The exact adapter+engine production consumer passes three fresh-process cold/warm64-track startup trials, with all6785 complete traced callbacks below2.667ms and first/full peaks1.327/2.410ms,1.351/1.828ms,1.371/2.317ms. Every phase proves worker termination before live host construction and identical received module handoff through full source verification. No live Wasm lazy/baseline compilation remains; do not extend that claim to every JS wrapper compilation event.

Two long-history runs each cross full source EOF for62 seconds then perform8 two-second seeks in cold/warm phases. All737280000 canonical bytes,64 meters, HTTP peak four and zero feed underruns/refusals/torn/errors remain proven. A warm coarse telemetry window in the first run reports3 late misses (peak4ms); focused tracing of the same eighth-seek segment in the second run finds all1510 callbacks under budget (cold/warm maxima1.086/1.104ms), with all456 telemetry windows clean. The earlier coarse-clock observation did not reproduce and remains unexplained; no speculative change hides it. Independent Astra xhigh review and the app report retain all evidence and physical iPad/Safari limits.

Root delivery remains: merge and OIDC-publish engine0.2.2, verify the registry archive/provenance, verify or regenerate the adapter's exact registry lock against that actual artifact, merge/publish adapter0.3.5, update the app's engine+adapter pins together and run required app gates. The current lock integrity is the reviewed local candidate's value, not evidence of existing registry publication. No npm publication or app deployment was performed by this implementation agent.
