[package]
name = "miso-blake3-wasm"
version = "0.1.0"
edition = "2024"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
blake3 = { version = "=1.8.7", default-features = false, features = ["wasm32_simd"] }

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
