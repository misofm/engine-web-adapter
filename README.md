# @misofm/engine-web-adapter

A headless, framework-neutral browser session adapter for
`@misofm/engine@0.1.0`.

The package is under active development. Callers own transport and decoding;
the adapter accepts canonical PCM streams through its codec-neutral stem API.
